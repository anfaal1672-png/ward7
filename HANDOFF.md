# 引き継ぎ

最終更新 2026-09-03 / 対象ブランチ `claude/ward7-handoff-rewkwi` / PR #1

## 0. 最初にやること

**コンテナは頻繁に巻き戻る（このプロジェクトで 9 回）。** ローカルの
`ward7.html` が 6548 行だったり `.tools/` が 5 個しか無ければ巻き戻っている。

```sh
cd /home/user/ward7
git fetch origin claude/ward7-handoff-rewkwi
git reset --hard origin/claude/ward7-handoff-rewkwi
wc -l ward7.html      # 12300 行前後なら正常
ls .tools | wc -l     # 58 前後
```

スクラッチパッドも同時に消える。`harness.js` だけは `.tools/` の版が古く
（`resetTime` が無い）**そのままでは計測できない**。作り直したら必ず
`.tools/` にコミットすること。これが失われると測定が全部止まる。

## 1. これは何か

単一ファイルのブラウザゲーム `ward7.html`（約 12,300 行）。
病棟からカルテを集めて脱出するホラー。AI 観戦モード（ボット）を内蔵。

**動かせない制約（守ること）**

- 単一ファイル。外部依存は CDN の three.js r128 のみ。ビルド無し
- 外部アセット 0。テクスチャは canvas で手続き生成、音は WebAudio で合成
- モバイル優先（タッチ操作、セーフエリア、`deviceMemory`/`hardwareConcurrency`
  による画質自動判定、解像度の追従）
- 章番号のコメント構成を維持し、新しいコードは該当章へ
- コメントは日本語で「なぜ」を書く（「何を」ではなく）。既存の語り口に合わせる
- `<script>` の中身は `(function(){...})()` に包まれている。外からは
  `window.__WARD7` と `window.__ward7SelfTest` だけ

**three.js r128 の落とし穴（何度も刺さっている）**

- 色管理が無い。`material.color` は sRGB ではなく線形値として使われる。
  `SRGB(hex)` で変換する
- `MeshStandardMaterial` は必ず 4% の鏡面反射（F0=0.04）を持つ。
  つや消し・繊維質は `MeshLambertMaterial` を使う
- `computeVertexNormals` の `normalizeNormals` はゼロ長を守らない。
  **NaN の法線ができ、iOS ではその部品が丸ごと描かれない**。
  必ず `fixNormals(g)` を通す（第 8 章）

## 2. 検証のやり方（必須）

1. `node .tools/extract.js ward7.html /tmp/w.js && node --check /tmp/w.js`
2. 未定義識別子の検査（acorn の AST。`undef.js` はスクラッチパッド側）
3. スタブでの実行（`harness.js` + `many.js`、`window.__WARD7` 経由）
4. **実ブラウザの自己診断**: `node .tools/stq.js /home/user/ward7/ward7.html`
   → 4 段階すべて FAIL 0 / エラー 0 であること（現在 q0,q1 79 項目・q2,q3 81 項目）
5. クリア率の測定: `sh .tools/runall2.sh <html> <難易度> <開始種> <本数> 4`
   → `node sum2.js out.txt`

**測定の注意**

- 60 本では何も判定できない（同じコードで 11.7%〜18.3% に振れた）。
  **240 本以上**で判定すること。σ は n=240・p=0.16 で 2.4 ポイント
- 4 コアで 240 本が約 76 秒
- 打ち切りは 800 秒で足りる（1300 秒に延ばしてもクリア数はほぼ変わらない）

**撮影（見た目を直すとき）**

- `node .tools/pin.js <html> [枚数] [間隔]` … **必ずこれを使う**。
  端末判定がこのコンテナで画質 0 を返すため、素直に開くと最低品質の姿しか
  撮れない。localStorage に q3 を入れてから開く。今回の見た目の修正 9 件は
  すべてこれに切り替えて初めて見えたもの
- `.tools/gait-shot.js` … 追跡者の歩容を横から。`?patrol=1` を足すと歩き
  （足さないと必ず追跡状態になり走りしか撮れない）
- `.tools/turn-shot.js` … 旋回中の姿勢
- `.tools/swing-shot.js` … 殴打の姿勢
- `crop.js`（切り出しと拡大。pngjs も ImageMagick も無いので Chromium の
  canvas を使う）はスクラッチパッド側にしか無い。必要なら作り直す

## 3. いまの数値

| | |
|---|---|
| 絶望のクリア率 | 18.8%（480 本）。開始時は 9.6% |
| 被弾 | 0.44 /100 秒（開始時 0.75） |
| 通常 / 静穏 | 37.5% / 85.0% |
| 自己診断 | 4 段階すべて FAIL 0（79 / 79 / 81 / 81 項目） |
| フレーム時間 | 390x844 dpr3 で中央値 398ms（SwiftShader。実機の値ではない） |

## 4. やり残し

### iPhone での確認（残ってはいるが、経路は塞いだ）

実機（iPhone/Safari）で「頭と手と衣服と足だけ見える」＝**体が消える**不具合が
報告された。原因は法線の NaN（three r128 の `normalizeNormals` がゼロ長を
守らず `1/0 × 0` になる）。実測で胴に 40 頂点・四肢の骨に各 6 頂点。

直し方は 3 段。生成の各所で `fixNormals` を通し、`buildHunter` の最後で
追跡者を掃き、さらに **`startGame` で `sanitizeNormals(scene)` と
`sanitizeNormals(viewScene)` を掛けてシーン全体を検める**。
最後の 1 段があるので、今後どの経路で形を作っても NaN の法線が GPU へ
出ていくことはない。

これが効くことは iPhone 無しで確かめられる。`loftGeo` の中の
`fixNormals(g)` をわざと外した版でも、自己診断「頂点属性に NaN が無い」は
4 段階すべて FAIL 0 のまま通る（＝網が拾っている）。

**それでも実機で見てもらう価値はある。** 直ったことの最終確認と、
NaN 以外の理由で消えている部品が無いかは、実機でしか分からない。

### 絶望のクリア率 90%（未達・おそらく到達不能）

33 通りを各 240 本（延べ 8000 本超）測って、基準を超えたのは 4 件だけ。
**被弾 × 走破時間が常に 2.1 前後に張り付く**。安全策は必ず同じだけ時間を食う。

- 追跡 5.722＋怒り 0.610 = 6.332 m/s に対し全力疾走 5.755 m/s。
  スタミナ込みの持続速度は 5.23 m/s。原理的に振り切れない
- 52 ダメージ × 2 で死亡・回復なし → 1 周で許される被弾は 1 発
- カルテは必要数ちょうどしか置かれず、踏破に 1356m。走破の下限は約 260 秒
- 90% には期待被弾 λ≦0.53 が要る（現在 2.1）
- **追跡者をプレイヤーより遅く・3 発耐える設定まで弱めてもクリアは 49.2%**。
  壁は難易度ではなくボットの走破効率にある

効かなかった案は全件、実測値つきで第 19 章のコメントに残してある。
同じ穴を掘らないこと。

### 追跡者のアニメーション（残 0 だが未撮影あり）

一新は完了。歩き・走り・旋回・立ち姿・殴打すべて撮って確認済み。
ただし**被弾直後の硬直（3 秒）とよろけは未撮影**。長いので目に付くはず。

### 見つけてあるが直していない不具合：硬直の姿勢

殴打後の硬直（3 秒）で**腕が頭上へ跳ね上がり、肘を折ったまま固まる**。
関節の詰まった人形に見える。ゲーム中いちばん長く姿勢が見える区間なので目立つ。

原因は特定済み。第 8 章 `updateHunter` の硬直ブロック（`hunter.stunT > 0 &&
hunter.swingT <= 0` の中）で、腕を「伸びきったまま垂れる」つもりで
`SA.up.rotation.x -= stunK * 1.15` と引いている。だが打ち抜いた直後の腕は
既に前方（`up.rotation.x = -1.62`）にあり、そこからさらに 1.15 引くと
-2.77rad ＝ 頭上になる。コメントの意図と符号が逆。

直し方は殴打の腕と同じで、基準姿勢に足すのではなく目標の角度へ寄せる
（骨は -Y 向きなので `rotation.x ≒ 0` が「垂れている」）。目安：
`SA.up.rotation.x = lerp(SA.up.rotation.x, 0.14, stunK)`、
`SA.fore.rotation.x = lerp(..., -0.28, stunK)`、`SB` は `stunK*0.7` で遅らせる。
のけぞり（`spine.rotation.x -= stunK*0.5`）も強すぎるので 0.28 程度に。

撮り方: `node .tools/swing-shot.js` を元に `h.stunT = 3.0` を立て、
毎フレームの `h.stunT = 0` を外す（スクラッチパッドの stun.js が同じもの）。

### そのほか

- 終盤（カルテ全回収後）の変換率が 58%。カルテ 6 枚到達が 35%、そこからの
  クリアが 58%
- 探索の実速度 4.1 m/s に対し持続上限は 5.23 m/s。ここを詰めれば走破時間が
  そのまま減るが、試した速度改善（横歩きを嫌う・首の回転を速く・回避の
  円錐の縁を選び直す）はいずれも壁こすりが増えて裏目に出た。
  `botFeet` の方向選択そのものを作り直す規模になる

## 5. 今回のセッションで分かった「作り方の穴」

1. **画質 0 のまま撮っていた**。最高品質の実物を一度も見ていなかった。
   → `pin.js` で塞いだ
2. **カメラ正面に置くと必ず追跡状態**になり、走りしか撮れていなかった。
   → `gait-shot.js` の `?patrol=1` で塞いだ
3. **デスクトップでは絵を見ても絶対に気づけない不具合がある**（法線 NaN）。
   → 自己診断「頂点属性に NaN が無い」で塞いだ（シーン全体を走査）
4. **60 本の測定は判定に使えない**。同じコードで 6 ポイント以上振れる

いずれも「見た／測った」つもりで見落としていた類。手順を疑うこと。
